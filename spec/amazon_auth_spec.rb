require 'spec_helper'

describe AmazonAuth do
  it 'has a version number' do
    expect(AmazonAuth::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
